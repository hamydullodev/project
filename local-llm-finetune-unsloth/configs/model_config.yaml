# Base model selection. Swap `active` to switch models without touching code.
active: llama3.2

models:
  llama3.2:
    repo_id: unsloth/Llama-3.2-3B-Instruct-bnb-4bit
    chat_template: llama-3.1
    max_seq_length: 2048
    gated: true # requires accepting Meta's license on Hugging Face + an HF token

  qwen2.5:
    repo_id: unsloth/Qwen2.5-7B-Instruct-bnb-4bit
    chat_template: qwen-2.5
    max_seq_length: 2048
    gated: false
