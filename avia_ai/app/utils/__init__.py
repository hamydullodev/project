"""Cross-cutting utilities: logging, caching, validation, and formatting.

These modules have no dependency on the agent or UI layers — they are pure,
reusable helpers that both can import safely without circular imports.
"""
