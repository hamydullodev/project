"""AI Travel Agent application package.

Root package for the Travel Agent app: a LangGraph-orchestrated,
locally-hosted (Ollama) LLM agent with real flight data tool calling,
rendered through a custom Streamlit UI.
"""
